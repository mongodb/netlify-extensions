{
  "name": "netlify-integrations",
  "description": "",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:util": "pnpm --filter util build",
    "build:git-changed-files": "pnpm build:util && pnpm --filter git-changed-files build",
    "lint": "biome lint",
    "format": "biome format",
    "lint:format": "pnpm lint && pnpm format",
    "fix-all": "biome lint --write && biome format --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "1.9.1"
  }
}
